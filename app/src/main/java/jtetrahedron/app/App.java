/*
 * This Java source file was generated by the Gradle 'init' task.
 */
package jtetrahedron.app;

import jtetrahedron.list.LinkedList;

import static jtetrahedron.utilities.StringUtils.join;
import static jtetrahedron.utilities.StringUtils.split;
import static jtetrahedron.app.MessageUtils.getMessage;

import org.apache.commons.text.WordUtils;

public class App {
    public static void main(String[] args) {
        LinkedList tokens;
        tokens = split(getMessage());
        String result = join(tokens);
        System.out.println(WordUtils.capitalize(result));
    }
}
